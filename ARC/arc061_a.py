#入力からsを文字列として取得する[s1s2s3...]
s = int(input())


#テスト用
#s=bin(3)
#print(s[1])

#+を入れる位置indexリストを作成する3。
#例：plus[1001]->0桁目、3桁目に+を入れる。len(plus)回for文を回す。
#plus_index
sum = 0

for i in range(len(s)):
    first = 
    second 

#合計を入れていくsum1



#sumを出力
